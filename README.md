# Diploma Software

Система анализа Bitcoin транзакций с веб-интерфейсом и интеграцией с LLM для выявления подозрительной активности.

## Описание

Проект представляет собой веб-приложение на Rust для анализа Bitcoin транзакций и адресов. Система извлекает и анализирует различные характеристики транзакций, визуализирует графы транзакций и интегрируется с Python-репозиторием, использующим LLM для классификации транзакций.

## Основные возможности

- **Анализ транзакций**: Извлечение и анализ характеристик Bitcoin транзакций
- **Анализ адресов**: Получение информации о Bitcoin адресах и их активности
- **Визуализация графов**: Построение графов транзакций с цветовой кодировкой
- **Веб-интерфейс**: Интуитивный веб-интерфейс для работы с системой
- **Интеграция с LLM**: Работа с Python-репозиторием для ML-анализа транзакций

## Структура проекта

```
diploma_software/
├── src/
│   ├── application/          # Бизнес-логика приложения
│   │   ├── services/          # Сервисы приложения
│   │   │   └── transaction/  # Сервисы для работы с транзакциями
│   │   └── plot_render.rs    # Рендеринг графиков
│   ├── infrastructure/       # Инфраструктурный слой
│   │   ├── constants.rs      # Константы приложения
│   │   └── model/            # Модели ML
│   ├── services/             # Внешние сервисы
│   │   └── explorer/         # Клиент для работы с Bitcoin explorer
│   ├── common/               # Общие утилиты
│   ├── utils/                # Вспомогательные функции
│   ├── data/                 # Данные для анализа
│   ├── main.rs               # Точка входа для CLI
│   └── main-api.rs           # Точка входа для веб-сервера
├── static/                   # Статические файлы (HTML шаблоны)
├── Cargo.toml                # Зависимости проекта
└── LICENSE                   # Лицензия Apache 2.0
```

## Требования

- Rust 1.70+ (edition 2021)
- Cargo
- Доступ к Bitcoin explorer API (настраивается через переменные окружения)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd diploma_software
```

2. Создайте файл `.env` в корне проекта (см. `.env.example` если есть):
```bash
# Пример конфигурации
BITCOIN_EXPLORER_URL=<your-explorer-url>
```

3. Соберите проект:
```bash
cargo build --release
```

## Использование

### Запуск веб-сервера

```bash
cargo run --bin main-api
```

Веб-сервер автоматически найдет свободный порт в заданном диапазоне и откроет браузер с главной страницей.

### Запуск CLI приложения

```bash
cargo run --bin main
```

## API Endpoints

- `GET /` - Главная страница
- `POST /confirm` - Анализ данных по папке
- `GET /tx/{tx_id}` - Информация о транзакции
- `GET /address/{address}` - Информация об адресе

## Интеграция с Python LLM

Проект разработан для работы с Python-репозиторием, который использует LLM для анализа транзакций. Интеграция осуществляется через:

- Обмен данными в формате JSON
- Использование общих моделей ML (например, `aml_bitcoin.pth`)
- API для передачи данных транзакций в Python-сервис

### Модель данных для Python сервиса

Rust приложение отправляет Python сервису следующие данные:

**Минимальный набор (текущая реализация):**
- `transaction_id` - ID транзакции
- `transaction_features` - базовые характеристики:
  - `n_inputs`, `n_outputs` - количество входов/выходов
  - `input_value_sum`, `output_value_sum` - суммы в BTC
  - `transaction_fee` - комиссия
  - `avg_input_value`, `avg_output_value` - средние значения

**Полный набор (планируется):**
- Все вышеперечисленное +
- `extended_features` - 15 дополнительных признаков
- `addresses` - списки входных и выходных адресов
- `input_features` / `output_features` - признаки для каждого адреса (55 признаков)

**Ожидаемый ответ от Python:**
```json
{
  "success": true,
  "prediction": {
    "class": "illicit" | "licit" | "unknown",
    "confidence": 0.0-1.0,
    "risk_score": 0.0-1.0
  },
  "explanation": "Текстовое объяснение"
}
```

📖 **Полная спецификация API**: см. [PYTHON_API_SPEC.md](PYTHON_API_SPEC.md) и [INTEGRATION.md](INTEGRATION.md)

## Разработка

### Зависимости

Основные зависимости проекта:
- `actix-web` - Веб-фреймворк
- `tokio` - Асинхронный runtime
- `reqwest` - HTTP клиент
- `bitcoin` - Работа с Bitcoin
- `blockbook` - Bitcoin explorer клиент
- `tera` - Шаблонизатор
- `plotly` - Визуализация данных

### Структура кода

Проект следует принципам чистой архитектуры:
- **Application** - бизнес-логика
- **Infrastructure** - инфраструктурные компоненты
- **Services** - внешние сервисы и интеграции

## Лицензия

Проект лицензирован под Apache License 2.0. См. файл [LICENSE](LICENSE) для деталей.

## Авторы

[Ваше имя/команда]

## Благодарности

- Bitcoin Core
- Blockbook Explorer
- Сообщество Rust

